apptainer {
        enabled = true
        runOptions = '--env PYTHONNOUSERSITE=1 --nv -B /hercules -B /mandap -B /mkfs -B /scratch'
        envWhitelist = 'APPTAINER_BINDPATH, APPTAINER_LD_LIBRARY_PATH'
    }